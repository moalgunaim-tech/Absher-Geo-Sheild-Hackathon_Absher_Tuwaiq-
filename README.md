# Absher Geo Shield – Hackathon Absher Tuwaiq  
فريق ميم فاء

حل تجريبي لتعزيز أمان تسجيل الدخول في منصة **أبشر** باستخدام:
- ربط حالة السفر وموقع المستخدم الحالي بمحاولات الدخول.
- تحليل سياق المحاولة (الدولة، نوع الجهاز، نوع الشبكة، IP).
- Threat Intel (VirusTotal + AlienVault OTX).
- Risk-Based Authentication (تدرج في المصادقة حسب مستوى الخطورة).
- مساعد ذكاء اصطناعي لتحليل الوضع الأمني والإحصائيات.

> هذا المشروع نموذج أولي (Proof of Concept) يمكن ربطه مستقبلاً بمنظومة أبشر الفعلية عبر واجهات برمجية رسمية.

---

## 🎯 الفكرة باختصار

بدل ما يكون تسجيل الدخول مجرد "اسم مستخدم + كلمة مرور"،  
النظام يحاول يجاوب على سؤال:  

> **"هل هذه المحاولة من صاحب الحساب فعلاً، وفي سياق منطقي؟"**

بمراعاة:

- هل المستخدم مسجّل كـ **مسافر** في أبشر أو داخل السعودية؟
- من **أي دولة** تأتي محاولة الدخول؟
- من **أي جهاز**؟ (جديد أو معروف)
- ما هو **نوع الشبكة**؟ (منزلية، جهة حكومية، شبكة عامة، VPN)
- ما هو **عنوان الـ IP**، وماذا تقول عنه منصات Threat Intel؟
- هل المستخدم أبلغ سابقاً أن هذه المحاولة **"ليست منه"**؟

ويتم على أساسها:

- إعطاء مستوى خطورة: (منخفض – متوسط – عالي).
- تحديد الإجراء:
  - منخفض → دخول مباشر.
  - متوسط → طلب خطوة تحقق إضافية (OTP).
  - عالي → حظر المحاولة + تنبيه.

---

## 🧱 المكونات الرئيسية

### 1. صفحة تسجيل الدخول التجريبية – `login.html`

- نموذج دخول بسيط:
  - مستخدم تجريبي: `demo@absher.gov.sa`
  - كلمة مرور: `P@ssw0rd!`
- حقول إضافية:
  - حالة المستخدم في أبشر (داخل المملكة / مسافر).
  - دولة السفر المسجّلة.
  - الدولة الحالية للمستخدم.
  - نوع الشبكة (منزلية / جهة عمل / عامة / VPN).
- عند كل محاولة (صحيحة أو خاطئة):
  - تُسجَّل المحاولة في `localStorage` تحت مفتاح `securityAnalytics`.
  - تُستخدم لاحقاً في لوحة الأمان و صفحة الإحصائيات.

### 2. لوحة الأمان (محرك المخاطر) – `dashboard.html`

- تحليل محاولات تسجيل الدخول بناءً على:
  - حالة السفر + الدولة المسجّلة.
  - الدولة الفعلية للمستخدم.
  - نوع الجهاز (معروف / جديد / غير معروف).
  - نوع الشبكة.
  - Threat Intel Tag (مثلاً: `Bruteforce_Global`, `Phishing_Campaign`…)
- حساب **درجة خطورة** رقمية + مستوى (منخفض / متوسط / عالي).
- سياسات مختلفة:
  - **Low** → يسمح بالدخول.
  - **Medium** → يطلب خطوة تحقق إضافية (OTP).
  - **High** → حظر المحاولة مع تنبيه.
- سيناريوهات جاهزة لأغراض العرض:
  - محاولة آمنة.
  - محاولة مشبوهة من دولة قريبة.
  - محاولة عالية الخطورة من دولة بعيدة + VPN + IP مشبوه.
- تخزين كل محاولة في `localStorage` ليتم عرضها في الإحصائيات.

### 3. صفحة الإحصائيات – `stats.html`

- ملخص أعلى الصفحة:
  - إجمالي المحاولات.
  - توزيع (منخفض / متوسط / عالي).
  - عدد بلاغات "هذا ليس أنا".
  - مستوى الأمان العام (1–10).
  - عدد الأيام بدون هجمة عالية.
- **توزيع المحاولات حسب الدول**:
  - كروت لكل دولة، مع عدد المحاولات وإجمالي المحاولات العالية.
- **توزيع المحاولات على ساعات اليوم**:
  - Histogram بسيط (0–23) لعرض أوقات النشاط.
- **Threat Intel Widget**:
  - تجميع المحاولات حسب IP.
  - عدد المحاولات لكل IP، أعلى مستوى خطورة، وآخر Threat Tag.
  - زر "تحليل IP" يفتح مودال يعرض:
    - GeoIP (الدولة، المدينة، مزود الخدمة).
    - مختصر نتائج VirusTotal (malicious / suspicious / reputation).
    - Pulses من AlienVault OTX (إن وجدت).
- **جدول المحاولات التفصيلي** مع فلاتر:
  - مستوى الخطورة.
  - الدولة.
  - عرض فقط المحاولات المبلّغ عنها "هذا ليس أنا".
- **مساعد ذكاء اصطناعي**:
  - استقبال سؤال بالعربي حول الإحصائيات.
  - إرسال السؤال + بيانات `securityAnalytics` إلى OpenAI.
  - استرجاع تحليل من نموذج GPT (تحليل مختصر + توصيات).

---

## 🏗️ المعمارية (Architecture)

- **Backend**
  - Node.js + Express.
  - ملفان رئيسيان:
    - `backend/server.js` → وضع **Online**:
      - يتصل فعلياً بـ:
        - OpenAI (Chat Completions).
        - VirusTotal (IP API v3).
        - AlienVault OTX (Indicators API).
        - ip-api.com (GeoIP).
    - `backend/server.offline.js` → وضع **Offline / Demo**:
      - يوفر نفس الـ API endpoints لكن ببيانات تجريبية ثابتة (Mock).
      - مفيد في حال عدم توفر الإنترنت في بيئة العرض.
  - Endpoints رئيسية:
    - `GET /api/services-status`  
      حالة الربط مع OpenAI / VT / OTX / GeoIP.
    - `POST /api/assistant`  
      سؤال للمساعد الأمني + JSON للإحصائيات.
    - `POST /api/ip-intel`  
      تحليل عنوان IP (GeoIP + VT + OTX) مع كاش داخلي للحد من الاستدعاءات.

- **Frontend**
  - ملفات ثابتة في `public/`:
    - `login.html`, `dashboard.html`, `stats.html`
  - تصميم مفصول:
    - CSS في `public/css/*.css`
    - JavaScript في `public/js/*.js`
  - تخزين الإحصائيات محلياً:
    - `localStorage.securityAnalytics`
    - لا يتم إرسال أي بيانات حساسة خارج المتصفح افتراضياً، إلا لغرض الـ Demo عند استخدام Threat Intel حقيقية.

---

## ⚙️ طريقة التشغيل (Local)

### 1. المتطلبات

- Node.js (يفضّل آخر إصدار LTS).
- حساب GitHub (اختياري للمشاركة).
- مفاتيح API (اختياري للوضع Online):
  - OpenAI API Key.
  - VirusTotal API Key.
  - AlienVault OTX API Key.

### 2. تنزيل المشروع

git clone https://github.com/moalgunaim-tech/Absher-Geo-Sheild-Hackathon_Absher_Tuwaiq-.git

